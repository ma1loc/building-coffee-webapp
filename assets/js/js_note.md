# <<< JS Notes taking >>>

document -> represents all the HTML loadedin browser

getElementById -> telling the browser to get an element in HTML that loaded by it's 'ID'

addEventListener -> JS methode used to listen in events like: "submit", "focus", "blur" 
    - submit: 

#### what i lean from js:
document
querySelector
getElementById
addEventListener
    submit -> used when a form is sent.
    focus -> happens when the user enters an input.
    blur -> 
Dynamic DOM Manipulation